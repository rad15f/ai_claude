export interface TextSignalResult {
  score: number
  signals: string[]
}

// Placeholder — logic implemented in Phase 2
export function scoreTextSignals(_text: string): TextSignalResult {
  return { score: 0, signals: [] }
}
